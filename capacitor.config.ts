import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyectoCacao',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
